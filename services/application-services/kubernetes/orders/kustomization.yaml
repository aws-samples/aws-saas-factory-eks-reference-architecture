# =========================================================================
# Order Service Kustomization
# =========================================================================
commonLabels:
  saas/tenant: "true"
  saas/service: order
resources:
- service.yaml
patchesJson6902:
# Deployment ServiceAccount patch
- path: patches/svc-acc-patch.yaml
  target:
    group: apps
    version: v1
    kind: Deployment
    name: order

images:
- name: KUSTOMIZE_IMAGE
